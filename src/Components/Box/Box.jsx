import styled from 'styled-components';
import { color, typography, layout, space, shadow, border, flexbox } from 'styled-system';

const Box = styled.div`
  ${color}
  ${typography}
  ${layout}
  ${space}
  ${shadow}
  ${border}
  ${flexbox}
`;

export default Box;
